have comments in an array, populate with data on first run the push comments on
submit

on delete press: get comment id, setcommentarray with filter, add everything
except matching id

on reply delete:
